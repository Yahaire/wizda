services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: wizda
    ports:
      - "5432:5432"
    volumes:
      - wizda_db:/var/lib/postgresql/data

volumes:
  wizda_db:
