/**
 * Reserved ad slot. Intentionally renders **nothing** for now — we don't show an
 * empty placeholder box to users. When a real ad (or house promo) is ready, drop
 * its markup in here; the surrounding layout already leaves room, so wiring it in
 * is a one-component change.
 */
export function AdSlot() {
  return null;
}
